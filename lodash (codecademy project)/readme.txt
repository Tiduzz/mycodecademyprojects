This project is the creation of the lodash functions library

It has the following functions, that belong to the lodash library:

- clamp(), inRange(), words(), pad(), has(), invert(), findKey(), drop(), dropWhile() and chunk()

Here is a link for the official lodash library documentation:
https://lodash.com/docs/4.17.15