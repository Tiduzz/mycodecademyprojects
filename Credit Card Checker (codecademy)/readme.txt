This project receives a array of digits, and has functions to:

- Validate the array as a valid credit card, through the Luhn Algorithm;
- Find and return the companies that emitted these credit cards.